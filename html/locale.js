function setLocale(loc)
{

    /* var l = Object.keys(JSON.parse(loc));
    for(var i = 0; i < l.length; i++)
    {

        console.log(l[i]);

    } */

    var l = JSON.parse(loc);
    locale = l;

}